{% extends '@SonataAdmin/CRUD/base_show_field.html.twig' %}

{% block field %}
    {% if value is not null %}
        {{ sonata_media(value, 'admin', {'class': 'img-thumbnail'}) }}
    {% endif %}
{% endblock %}
